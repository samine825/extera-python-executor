## Exteragram Python Executor Plugin
The plugin allows you to execute Python code directly in the chat.
• .p <code> — execute the code
• .i <text>/<response to a message> — input input()
• .stop — interrupt execution"""
